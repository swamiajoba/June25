import { LiveScorePipe } from './live-score.pipe';

describe('LiveScorePipe', () => {
  it('create an instance', () => {
    const pipe = new LiveScorePipe();
    expect(pipe).toBeTruthy();
  });
});
